[package]
name = "topology"
version = "0.2.0"
edition = "2024"

[features]
topology-tests = []

[dev-dependencies]
reqwest   = { version = "0.13.3", default-features = false, features = ["json", "rustls"] }
serde_json = "1"
tokio     = { version = "1", features = ["full"] }

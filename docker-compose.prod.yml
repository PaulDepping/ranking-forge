services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-rankingforge}
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  api:
    image: ghcr.io/pauldepping/ranking-forge/api:edge
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-rankingforge}
      STARTGG_API_KEY: ${STARTGG_API_KEY}
      CORS_ORIGIN: ${CORS_ORIGIN:-https://rankingforge.com}
      RUST_LOG: ${RUST_LOG:-warn}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  worker:
    image: ghcr.io/pauldepping/ranking-forge/worker:edge
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-rankingforge}
      STARTGG_API_KEY: ${STARTGG_API_KEY}
      RUST_LOG: ${RUST_LOG:-warn}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  crawler:
    image: ghcr.io/pauldepping/ranking-forge/crawler:edge
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-rankingforge}
      STARTGG_API_KEY: ${STARTGG_API_KEY}
      RUST_LOG: ${RUST_LOG:-warn}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
    depends_on:
      - api
      - web
    restart: unless-stopped

  web:
    image: ghcr.io/pauldepping/ranking-forge/web:edge
    environment:
      ORIGIN: ${ORIGIN:-https://rankingforge.com}
      PUBLIC_API_URL: ${PUBLIC_API_URL:-https://api.rankingforge.com}
      INTERNAL_API_URL: ${INTERNAL_API_URL:-http://api:3000}
      COOKIE_DOMAIN: ${COOKIE_DOMAIN}
    depends_on:
      - api
    restart: unless-stopped

volumes:
  postgres_data:
  caddy_data:
